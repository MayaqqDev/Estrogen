<?xml version="1.0" encoding="UTF-8"?>
<!-- Copy of the fabric log4j config, you can find the original at https://github.com/FabricMC/fabric-loom/blob/fd8cc101203a58b44187dbc4cf4509fcf45e8ccb/src/main/resources/log4j2.fabric.xml -->
<!-- All credits belong to them -->
<Configuration status="WARN">
    <Appenders>

        <!--	System out	-->
        <Console name="SysOut" target="SYSTEM_OUT">
            <Filters>
                <!-- Filter out the authentication errors when starting in development -->
                <RegexFilter regex="^Failed to verify authentication$" onMatch="DENY" onMismatch="NEUTRAL"/>
                <RegexFilter regex="^Failed to fetch user properties$" onMatch="DENY" onMismatch="NEUTRAL"/>
                <RegexFilter regex="^Couldn't connect to realms$" onMatch="DENY" onMismatch="NEUTRAL"/>
                <RegexFilter regex="^Failed to fetch Realms feature flags$" onMatch="DENY" onMismatch="NEUTRAL"/>
                <!-- Filter out data fixer errors for entities -->
                <RegexFilter regex="^No data fixer registered for .*$" onMatch="DENY" onMismatch="NEUTRAL"/>
                <!-- Quickly filter out bake models so the log is actually navigable, Changed onMatch="NEUTRAL" to onMatch="DENY" -->
                <RegexFilter regex="^Unable to bake model: .*: java.util.NoSuchElementException$" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
            </Filters>
            <PatternLayout>
                <LoggerNamePatternSelector
                    defaultPattern="%style{[%d{HH:mm:ss}]}{blue} %highlight{[%t/%level]}{FATAL=red, ERROR=red, WARN=yellow, INFO=green, DEBUG=cyan, TRACE=blue} %style{(%logger{1})}{magenta} %highlight{%msg%n}{FATAL=red, ERROR=red, WARN=normal, INFO=normal, DEBUG=normal, TRACE=normal}"
                    disableAnsi="false">
                    <!-- Don't show the logger name for minecraft classes-->
                    <PatternMatch key="net.minecraft.,com.mojang."
                                  pattern="%style{[%d{HH:mm:ss}]}{blue} %highlight{[%t/%level]}{FATAL=red, ERROR=red, WARN=yellow, INFO=green, DEBUG=green, TRACE=blue} %style{(Minecraft)}{magenta} %highlight{%msg{nolookups}%n}{FATAL=red, ERROR=red, WARN=normal, INFO=normal, DEBUG=normal, TRACE=normal}"/>
                </LoggerNamePatternSelector>
            </PatternLayout>
        </Console>

        <!--	Vanilla server gui	-->
        <Queue name="ServerGuiConsole" ignoreExceptions="true">
            <PatternLayout>
                <LoggerNamePatternSelector defaultPattern="[%d{HH:mm:ss} %level] (%logger{1}) %msg{nolookups}%n">
                    <!-- Don't show the logger name for minecraft classes-->
                    <PatternMatch key="net.minecraft.,com.mojang." pattern="[%d{HH:mm:ss} %level] %msg{nolookups}%n"/>
                </LoggerNamePatternSelector>
            </PatternLayout>
        </Queue>

        <!--	latest.log same as vanilla	-->
        <RollingRandomAccessFile name="LatestFile" fileName="logs/latest.log" filePattern="logs/%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <LoggerNamePatternSelector defaultPattern="[%d{HH:mm:ss}] [%t/%level] (%logger{1}) %msg{nolookups}%n">
                    <!-- Don't show the logger name for minecraft classes-->
                    <PatternMatch key="net.minecraft.,com.mojang." pattern="[%d{HH:mm:ss}] [%t/%level] (Minecraft) %msg{nolookups}%n"/>
                </LoggerNamePatternSelector>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <OnStartupTriggeringPolicy/>
            </Policies>
        </RollingRandomAccessFile>

        <!--	Debug log file	-->
        <RollingRandomAccessFile name="DebugFile" fileName="logs/debug.log" filePattern="logs/debug-%i.log.gz">
            <PatternLayout pattern="[%d{HH:mm:ss}] [%t/%level] (%logger) %msg{nolookups}%n"/>

            <!--	Keep 5 files max	-->
            <DefaultRolloverStrategy max="5" fileIndex="min"/>

            <Policies>
                <SizeBasedTriggeringPolicy size="200MB"/>
                <OnStartupTriggeringPolicy/>
            </Policies>

        </RollingRandomAccessFile>
    </Appenders>
    <Loggers>
        <Logger level="info" name="net.minecraft"/>
        <Root level="all">
            <AppenderRef ref="DebugFile" level="debug"/>
            <AppenderRef ref="SysOut" level="info"/>
            <AppenderRef ref="LatestFile" level="info"/>
            <AppenderRef ref="ServerGuiConsole" level="info"/>
        </Root>
        <Logger level="all" name="Estrogen">
            <AppenderRef ref="DebugFile" level="debug"/>
            <AppenderRef ref="SysOut" level="all"/>
            <AppenderRef ref="LatestFile" level="all"/>
            <AppenderRef ref="ServerGuiConsole" level="info"/>
        </Logger>
    </Loggers>
</Configuration>
